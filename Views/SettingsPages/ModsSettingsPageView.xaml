<local:SettingsPageView x:Class="GohMdlExpert.Views.SettingsPages.ModsSettingsPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:GohMdlExpert.Views.SettingsPages" 
             xmlns:viewmodel="clr-namespace:GohMdlExpert.ViewModels.SettingsPages" 
             d:DataContext="{d:DesignInstance Type=viewmodel:ModsSettingsPageViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <DataGrid ItemsSource="{Binding Mods}" AutoGenerateColumns="False" SelectedItem="{Binding SelectedMod}" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="On" Binding="{Binding IsEnable}" Width="30"/>
            
                <DataGridTemplateColumn MinWidth="100">
                    <DataGridTemplateColumn.Header>
                        <TextBlock Text="Name" TextAlignment="Center" HorizontalAlignment="Right"/>
                    </DataGridTemplateColumn.Header>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="Path" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Path}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        
        <StackPanel  Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="1">
            <Button Content="Add" Command="{Binding AddModCommand}" Height="30" Width="100" Margin="5" Padding="5"/>
            <Button Content="Remove" Command="{Binding RemoveModCommand}" Height="30" Width="100" Margin="5" Padding="5"/>
            <Button Content="Approve" Command="{Binding ApproveCommand}" Height="30" Width="100" Margin="5" Padding="5"/>
        </StackPanel>
    </Grid>
</local:SettingsPageView>
