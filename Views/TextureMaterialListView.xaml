<bv:BaseView x:Class="GohMdlExpert.Views.TextureMaterialListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:bv="clr-namespace:WpfMvvm.Views;assembly=WpfMvvm" 
             xmlns:local="clr-namespace:GohMdlExpert.Views"
             xmlns:vm="clr-namespace:GohMdlExpert.ViewModels" 
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vm:TextureMaterialListViewModel}">
    <DockPanel>
        <TextBlock d:Text="Обозреватель материалов текстуры: texture.mtl" Text="{Binding MtlFile.Name, StringFormat='Texture materials overview: {0}'}" DockPanel.Dock="Top"/>
        <ListView Grid.Row="1" ItemsSource="{Binding Items, UpdateSourceTrigger=PropertyChanged}" SelectedIndex="{Binding SelectedMaterialIndex}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <Image Source="/Resources/texture.ico" Width="20" />
                        <TextBlock Text="{Binding Diffuse.Name}"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </DockPanel>
</bv:BaseView>
