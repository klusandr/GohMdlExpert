<bv:BaseView x:Class="GohMdlExpert.Views.DefaultMaterialView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GohMdlExpert.Views"
             xmlns:bv="clr-namespace:WpfMvvm.Views;assembly=WpfMvvm" xmlns:viewmodels="clr-namespace:GohMdlExpert.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:DefaultMaterialViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="200">
    <StackPanel Margin="5">
        <TextBlock Text="Default material"/>
        <Image Source="{Binding MaterialFile.ImageSource}" MaxWidth="150" MaxHeight="150"/>
        <TextBlock Text="{Binding MaterialFile.Name}"/>
        <Button Content="Select default material" Command="{Binding SelectMaterialCommand}" Margin="5" Padding="5"/>
        <CheckBox Content="Use when no material" IsChecked="{Binding IsUse}"/>
        <CheckBox Content="Use always"  IsChecked="{Binding IsUseAlways}" IsEnabled="{Binding IsUse}"/>
    </StackPanel>
</bv:BaseView>
