<bv:BaseView x:Class="GohMdlExpert.Views.MaterialLoadView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GohMdlExpert.Views"
             xmlns:bv="clr-namespace:WpfMvvm.Views;assembly=WpfMvvm"
             xmlns:trees="clr-namespace:GohMdlExpert.Views.Trees" xmlns:viewmodels="clr-namespace:GohMdlExpert.ViewModels" 
             d:DataContext="{d:DesignInstance Type=viewmodels:MaterialLoadViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width=".4*"/>
            
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <trees:MaterialLoadTreeView DataContext="{Binding MaterialTree}"/>
        <GridSplitter Width="3"/>
        <Image Source="{Binding SelectedMaterialFile.ImageSource}" Grid.Column="2"/>
        
        <StackPanel Grid.Row="1" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Command="{Binding CancelCommand}" Margin="5" Width="70"/>
            <Button Content="Apply" Command="{Binding ApplyCommand}" Margin="5" Width="70"/>
            <Button Content="ОК" Command="{Binding ApproveCommand}" Margin="5" Width="70"/>
        </StackPanel>
    </Grid>
</bv:BaseView>
