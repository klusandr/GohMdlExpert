<views:BaseView x:Class="GohMdlExpert.Views.Dialogs.SelectResourceFileDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GohMdlExpert.Views.Dialogs"
             xmlns:views="clr-namespace:WpfMvvm.Views;assembly=WpfMvvm"
             xmlns:controlsView="clr-namespace:WpfMvvm.Views.Controls;assembly=WpfMvvm"
             xmlns:controls="clr-namespace:WpfMvvm.Controls;assembly=WpfMvvm"
             xmlns:viewmodel="clr-namespace:GohMdlExpert.ViewModels.Dialogs" xmlns:trees="clr-namespace:GohMdlExpert.Views.Trees"
                d:DataContext="{d:DesignInstance Type=viewmodel:SelectResourceFileDialogViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.5*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <controlsView:TreeView DataContext="{Binding Tree}">
            <controlsView:TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Items}">
                    <trees:ResourceLoadFileTreeItemView/>
                </HierarchicalDataTemplate>
            </controlsView:TreeView.ItemTemplate>
        </controlsView:TreeView>

        <StackPanel Grid.Column="1" Margin="10">
            <TextBlock Text="Selected resource:" FontSize="16"/>
            <TextBox Text="{Binding SelectedResourceFullName, Mode=OneWay}" TextWrapping="Wrap" IsReadOnly="True" FontSize="14" Margin="10 0 0 0"/>
        </StackPanel>
        
        <StackPanel Style="{StaticResource DialogButtonStackPanel}" Grid.Row="1" Grid.ColumnSpan="2">
            <Button Content="Select" Command="{Binding SelectCommand}" IsDefault="True"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}" IsCancel="True"/>
        </StackPanel>
    </Grid>
</views:BaseView>
